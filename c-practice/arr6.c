#include <stdio.h>

/**
 * main - prints a string
 *
 * Return: 0 successfully.
 */
int main(void)
{
	char message[] = "Good to see you,";
	char fname[] = "John";

	printf("%s %s!\n", message, fname);

	return (0);
}
